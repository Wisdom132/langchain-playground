let _langchain_core_messages = require("@langchain/core/messages");
require("uuid");

//#region src/utils.ts
function extractBase64FromDataUrl(dataUrl) {
	const match = dataUrl.match(/^data:.*?;base64,(.*)$/);
	return match ? match[1] : "";
}
function convertAIMessageToCerebras(messages) {
	const toolCalls = messages.tool_calls?.map((tc) => ({
		id: tc.id,
		type: "function",
		function: {
			name: tc.name,
			arguments: JSON.stringify(tc.args)
		}
	}));
	if (typeof messages.content === "string") {
		if (messages.tool_calls?.length) return [{
			role: "assistant",
			content: messages.content,
			tool_calls: toolCalls
		}];
		return [{
			role: "assistant",
			content: messages.content
		}];
	}
	const textMessages = messages.content.filter((c) => c.type === "text" && typeof c.text === "string").map((c) => ({
		role: "assistant",
		content: c.text
	}));
	let toolCallMsgs;
	if (messages.content.find((c) => c.type === "tool_use") && messages.tool_calls?.length) {
		if (toolCalls) toolCallMsgs = {
			role: "assistant",
			tool_calls: toolCalls,
			content: ""
		};
	} else if (messages.content.find((c) => c.type === "tool_use") && !messages.tool_calls?.length) throw new Error("'tool_use' content type is not supported without tool calls.");
	return [...textMessages, ...toolCallMsgs ? [toolCallMsgs] : []];
}
function convertHumanGenericMessagesToCerebras(message) {
	if (typeof message.content === "string") return [{
		role: "user",
		content: message.content
	}];
	return message.content.map((c) => {
		if (c.type === "text") return {
			role: "user",
			content: c.text
		};
		else if (c.type === "image_url") {
			if (typeof c.image_url === "string") return {
				role: "user",
				content: "",
				images: [extractBase64FromDataUrl(c.image_url)]
			};
			else if (typeof c.image_url === "object" && c.image_url !== null && "url" in c.image_url && typeof c.image_url.url === "string") return {
				role: "user",
				content: "",
				images: [extractBase64FromDataUrl(c.image_url.url)]
			};
		}
		throw new Error(`Unsupported content type: ${c.type}`);
	});
}
function convertSystemMessageToCerebras(message) {
	if (typeof message.content === "string") return [{
		role: "system",
		content: message.content
	}];
	else if (message.content.every((c) => c.type === "text" && typeof c.text === "string")) return message.content.map((c) => ({
		role: "system",
		content: c.text
	}));
	else throw new Error(`Unsupported content type(s): ${message.content.map((c) => c.type).join(", ")}`);
}
function convertToolMessageToCerebras(message) {
	if (typeof message.content !== "string") throw new Error("Non string tool message content is not supported");
	return [{
		role: "tool",
		content: message.content,
		tool_call_id: message.tool_call_id
	}];
}
function convertToCerebrasMessageParams(messages) {
	return messages.flatMap((msg) => {
		if (["human", "generic"].includes(msg.type)) return convertHumanGenericMessagesToCerebras(msg);
		else if (msg.type === "ai") return convertAIMessageToCerebras(msg);
		else if (msg.type === "system") return convertSystemMessageToCerebras(msg);
		else if (msg.type === "tool") return convertToolMessageToCerebras(msg);
		else throw new Error(`Unsupported message type: ${msg.type}`);
	});
}
function formatToCerebrasToolChoice(toolChoice) {
	if (!toolChoice) return;
	else if (toolChoice === "any" || toolChoice === "required") return "required";
	else if (toolChoice === "auto") return "auto";
	else if (toolChoice === "none") return "none";
	else if (typeof toolChoice === "string") return {
		type: "function",
		function: { name: toolChoice }
	};
	else return toolChoice;
}

//#endregion
exports.convertToCerebrasMessageParams = convertToCerebrasMessageParams;
exports.formatToCerebrasToolChoice = formatToCerebrasToolChoice;
//# sourceMappingURL=utils.cjs.map